name: Benchmarks

# Criterion benches run here, parallel to the gates in ci.yml. The test job
# excludes bench *executions* (their test-mode setup dominates runtime and
# yields no test coverage), so this workflow is where they run for real:
# release builds, full criterion measurement.
#
# Runs per code PR/push (measured ~7 minutes, inside the 30-minute promotion
# rule) and keeps the weekly scheduled full run (wayfinder #166
# re-evaluation). A benchmark contributes no gate: it never blocks, and the
# weekly run is the stable measurement baseline.
on:
  push:
    branches: [main]
    paths:
      - "crates/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
  pull_request:
    branches: [main]
    paths:
      - "crates/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
  schedule:
    - cron: "0 6 * * 1" # Weekly, Monday 06:00 UTC
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read

concurrency:
  group: bench-${{ github.ref }}
  cancel-in-progress: true

jobs:
  bench:
    name: Criterion benches
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@02cb101ec7c40f2c49e1d9714d64511d8e1b74de # master
        with:
          toolchain: 1.98.1

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          key: ${{ github.job }}
          shared-key: rust-cache
          cache-on-failure: true

      # -p keeps the build to the bench crates and their dependencies, so
      # protoc (needed only by glasschain-rpc) is not required here.
      - name: Run benches
        run: |
          cargo bench -p glasschain-core
          cargo bench -p glasschain-vm
          cargo bench -p glasschain-workflows
